# GENERATED by ptn-gen from the pantheon-go registry. Do not edit.
# Source of truth is the Go action Spec; regenerate with `ptn-gen emit`.

kind: task
name: finance.normalize_fx
version: 1
summary: "Convert every ledger amount into a single base currency."

runner:
  kind: script
  runtime: go
  entry: finance.normalize_fx

input: finance.FXNormalizeRequest@1
output: finance.NormalizedLedger@1

uses:
  - name: fx
    verbs: [request]

policy:
  timeout: "1m0s"
  retry: 2
  idempotent: true
