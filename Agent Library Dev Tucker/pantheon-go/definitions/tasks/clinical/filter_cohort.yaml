# GENERATED by ptn-gen from the pantheon-go registry. Do not edit.
# Source of truth is the Go action Spec; regenerate with `ptn-gen emit`.

kind: task
name: clinical.filter_cohort
version: 1
summary: "Select the patients matching clinical criteria that the requester may see."

runner:
  kind: script
  runtime: go
  entry: clinical.filter_cohort

input: clinical.CohortQuery@1
output: clinical.Cohort@1

uses:
  - name: records
    verbs: [query]

policy:
  timeout: "1m0s"
  retry: 2
  idempotent: true
