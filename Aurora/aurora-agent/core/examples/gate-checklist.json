{
  "id": "G-E2E",
  "run": "r-2026-08-02-ex01",
  "stage": "deliver",
  "shape": "checklist",
  "blocking": true,
  "prompt": "User-facing behaviour changed: a full runs volume now fails loudly instead of silently dropping journal lines. Tick each end-to-end check you ran against the ephemeral stack before this goes to PR; leave unticked anything that did not pass.",
  "context": {
    "pr": "aurora-agent#128",
    "surface": "operator CLI output and the run journal on disk",
    "environment": "ns-r-2026-08-02-ex01, runs volume capped at 1MiB"
  },
  "items": [
    "Ran a full pipeline with the runs volume full: the run aborts instead of continuing",
    "The operator sees an error naming the runs directory, not a bare ENOSPC traceback",
    "journal.jsonl has no truncated final line after the failure",
    "Every line written before the failure still parses as JSON",
    "Freed space and re-ran: the run completes and the journal is well-formed"
  ],
  "status": "open",
  "answer": null
}
